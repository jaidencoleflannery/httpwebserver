using System.IO;
using System.Net;

namespace Server.Router;

public class Router
{
/*
    public static void RouteRequest(HttpListenerRequest request)
    {
        request.RawUrl
    }
    public static byte[] Get()
    {
        byte[] document = System.IO.File.ReadAllBytes("./Content/Pages/index.html");
        return document;
    }
    */
}